# ZEUS Rebuttal Response Draft

Usage note: this draft only uses completed experiments. It is written to be compact enough for a 5000-character rebuttal after trimming reviewer IDs and optional sentences.

## Main Tables To Include

### Table 1: FLUX semantic alignment, preference, and latency

Quality is measured on 200 COCO prompts. Latency is measured in a separate same-GPU sequential pass on 20 COCO prompts, FLUX.1-dev, 50 steps, batch size 1.

| Method | CLIPScore ↑ | ImageReward ↑ | sec/img ↓ | Speedup ↑ |
|---|---:|---:|---:|---:|
| Full | 0.3116 | 1.0991 | 25.84 | 1.00x |
| SADA | 0.3113 | 1.0808 | 10.55 | 2.45x |
| ZEUS | 0.3112 | 1.0930 | 10.51 | 2.46x |

Key sentence:

> On FLUX.1-dev, ZEUS matches Full on CLIPScore within 0.0004 and keeps ImageReward within 0.0061, while achieving 2.46x same-GPU wall-clock speedup. At essentially the same latency as SADA, ZEUS obtains a higher ImageReward in this setting.

### Table 2: Wan2.1 balanced VBench-24

Setting: Wan2.1-T2V-14B, 480x832, 81 frames, 50 steps, VBench 0.1.5. The subset contains 24 prompts: 8 from subject consistency, 8 from motion smoothness, and 8 from temporal flickering. This table is for video quality only; videos were generated by distributing different prompts across GPUs, while each video was still generated on a single GPU.

| Method | Videos | Subject ↑ | Motion ↑ | Flicker ↑ | Imaging ↑ |
|---|---:|---:|---:|---:|---:|
| Full | 24 | 0.9619 | 0.9810 | 0.9737 | 0.6118 |
| ZEUS | 24 | 0.9628 | 0.9820 | 0.9755 | 0.6008 |
| Delta | - | +0.0010 | +0.0010 | +0.0018 | -0.0110 |

Key sentence:

> The VBench results directly address the concern that modifying the ODE trajectory may harm temporal behavior: ZEUS slightly improves subject consistency, motion smoothness, and temporal flickering on this balanced subset, with only a small imaging-quality drop.

### Table 3: Reviewer concern map

| Concern | Response | Evidence / action |
|---|---|---|
| Missing FLUX alignment / preference metrics | Add CLIPScore and ImageReward. ZEUS preserves semantic alignment and preference quality. | Table 1 |
| Missing video temporal benchmark | Add VBench temporal metrics on Wan2.1. ZEUS does not degrade subject/motion/flicker. | Table 2 |
| SADA / adaptive methods fairness | Clarify that ZEUS is not claimed to subsume adaptive schedulers. Compare by real wall-clock latency, not nominal acceleration labels. | Table 1 same-GPU timing; main paper latency tables |
| SADA stronger on some SD-family points | Acknowledge this. Position ZEUS as a simpler, output-level, architecture-agnostic tradeoff rather than pointwise domination. | Textual clarification |
| Complementarity with token/attention sparsity | Soften to "potentially complementary"; add same-setting sparse-attention and conservative token-update pilots as error-composition sanity checks. | Optional Tables 7-8; residual-norm token stress is a negative control |
| Training-free wording and fixed schedule | Clarify "training-free" means no retraining, learned policy, or per-model calibration; the fixed window is a deterministic recipe. | Textual clarification |
| Full VBench benchmark | Do not claim full VBench. State that the rebuttal run uses a balanced VBench-24 subset targeting the reviewer-named temporal dimensions. | Table 2 caption |

### Optional Table 4: SDXL matched-budget SADA comparison

Quality uses 200 COCO prompts. Timing is a separate same-GPU sequential pass on 50 prompts.

| Method | Actual NFE | sec/img ↓ | Speedup ↑ | CLIPScore ↑ | ImageReward ↑ |
|---|---:|---:|---:|---:|---:|
| Full | 50.0 | 3.366 | 1.00x | 0.3170 | 0.7550 |
| SADA | 25.2 | 1.556 | 2.16x | 0.3171 | 0.7417 |
| ZEUS | 24.0 | 1.464 | 2.30x | 0.3166 | 0.7339 |

Key sentence:

> On SDXL, ZEUS and SADA are closely matched in actual NFE and same-GPU latency. SADA has slightly higher ImageReward, while ZEUS is faster with nearly identical CLIPScore; we will present this as a deployment tradeoff rather than pointwise dominance.

### Optional Table 5: SDXL schedule and predictor robustness

| Experiment | Best rebuttal takeaway |
|---|---|
| Predictor ablation | Lagrange-4 does not improve over the default; no-Lagrange is more conservative and improves ImageReward at higher NFE/latency. |
| Window sensitivity | CLIPScore is stable across 10/80/10, 15/70/15, 20/70/10, and 25/60/15; ImageReward follows the expected speed-quality tradeoff. |
| r stress test | CLIPScore remains stable for r=3 to r=6, while ImageReward drops at the boundary, clarifying the practical acceleration regime. |

### Optional Table 6: Consecutive-skip boundary

Setting: SDXL base 1.0, 200 COCO prompts, 1024x1024, 50 steps, DPM solver, guidance 5.0, batch size 1. Generation and evaluation were run sequentially on GPU2. Default rows sweep only `max_interval`; extended-anchor rows increase `lagrange_int` to allow longer observed consecutive skip runs.

| Schedule | max_interval | lagrange_int | Observed max consecutive skips | NFE | sec/img | CLIP ↑ | ImageReward ↑ |
|---|---:|---:|---:|---:|---:|---:|---:|
| Default | 2 | 6 | 2 | 27.0 | 1.625 | 0.3169 | 0.7450 |
| Default | 4 | 6 | 4 | 27.0 | 1.636 | 0.3171 | 0.7631 |
| Default | 6 | 6 | 5 | 24.0 | 1.463 | 0.3166 | 0.7339 |
| Default | 8 | 6 | 5 | 24.0 | 1.469 | 0.3166 | 0.7339 |
| Extended | 8 | 8 | 7 | 23.0 | 1.432 | 0.3167 | 0.7366 |
| Extended | 12 | 12 | 11 | 22.0 | 1.346 | 0.3133 | 0.5599 |

Key sentence:

> Under the default anchor schedule, increasing max_interval beyond 6 saturates at 5 observed consecutive skips and does not further change quality. When we explicitly extend the anchor interval, 7 consecutive skips remains close to default quality, while 11 consecutive skips causes a large ImageReward drop, identifying the practical failure boundary.

### Optional Table 7: Sparse-attention complementarity pilot

Setting: SDXL base 1.0, 200 COCO prompts, 1024x1024, 50 steps, DPM solver, guidance 5.0, batch size 1. The sparse variant is a naive top-k cross-attention sparsity pilot, not an optimized sparse-attention speed baseline.

| Method | Cross-attn sparsity | NFE | CLIP ↑ | ImageReward ↑ |
|---|---:|---:|---:|---:|
| Full | 0% | 50.0 | 0.3170 | 0.7550 |
| ZEUS | 0% | 24.0 | 0.3166 | 0.7339 |
| Sparse only | 20% | 50.0 | 0.3176 | 0.7411 |
| ZEUS + sparse | 20% | 24.0 | 0.3170 | 0.7114 |
| Sparse only | 50% | 50.0 | 0.3173 | 0.7717 |
| ZEUS + sparse | 50% | 24.0 | 0.3171 | 0.7591 |

Key sentence:

> A naive cross-attention sparsity pilot shows stable CLIPScore for both sparse-only and ZEUS+sparse settings, suggesting no prompt-alignment collapse. ImageReward changes are modest except for the 20% sparse composition case; we will present this only as an error-composition sanity check, not as evidence of optimized sparse-attention speedup.

### Optional Table 8: Conservative token-update sparsity pilot

Setting: SDXL base 1.0, 200 COCO prompts, 1024x1024, 50 steps, DPM solver, guidance 5.0, batch size 1. This token-update proxy freezes only the lowest-update tokens according to per-block `||output - input||`. It is not an optimized token-pruning speed baseline because the full block is still computed before masking.

| Method | Token-update sparsity | NFE | CLIP ↑ | ImageReward ↑ |
|---|---:|---:|---:|---:|
| Full | 0% | 50.0 | 0.3170 | 0.7550 |
| ZEUS | 0% | 24.0 | 0.3166 | 0.7339 |
| Token-update only | 5% | 50.0 | 0.3168 | 0.7157 |
| ZEUS + token-update | 5% | 24.0 | 0.3163 | 0.6910 |
| Token-update only | 10% | 50.0 | 0.3181 | 0.6815 |
| ZEUS + token-update | 10% | 24.0 | 0.3178 | 0.6291 |

Key sentence:

> A conservative update-norm token-update proxy avoids the collapse seen with residual-norm token freezing: 5% pruning keeps CLIPScore essentially unchanged and has a moderate ImageReward drop under both Full and ZEUS. At 10%, CLIPScore remains stable but ImageReward becomes more sensitive, so we present this as a lightweight compatibility sanity check rather than a token-pruning speed baseline.

### Optional Table 9: Residual-norm token-update negative control

This table is mainly for internal/appendix use. It is aligned with Optional Table 7 and uses the same 200 COCO prompts. The token rule keeps high input/residual-norm tokens, which is a crude negative control rather than a redundancy-aware pruning rule. Each quality cell is `CLIPScore / ImageReward`.

| Pruned fraction | Sampler | Base | + sparse-attn | + residual-token | + sparse-attn + residual-token |
|---:|---|---:|---:|---:|---:|
| 20% | Full | 0.3170 / 0.7550 | 0.3176 / 0.7411 | 0.3035 / 0.1245 | 0.3029 / 0.1389 |
| 20% | ZEUS | 0.3166 / 0.7339 | 0.3170 / 0.7114 | 0.3013 / 0.0333 | 0.3004 / 0.0256 |
| 50% | Full | 0.3170 / 0.7550 | 0.3173 / 0.7717 | 0.2569 / -1.7527 | 0.2570 / -1.7408 |
| 50% | ZEUS | 0.3166 / 0.7339 | 0.3171 / 0.7591 | 0.2526 / -1.7956 | 0.2521 / -1.8004 |

## Draft Rebuttal Text

We thank the reviewers for the concrete suggestions. We added two sets of experiments to address the main missing evidence: standard text-to-image alignment/preference metrics and video temporal metrics.

First, for FLUX.1-dev, we evaluated CLIPScore and ImageReward on 200 COCO prompts and also ran a clean same-GPU timing pass on 20 prompts. Full sampling obtains 0.3116 CLIPScore and 1.0991 ImageReward at 25.84 sec/img. ZEUS obtains 0.3112 CLIPScore and 1.0930 ImageReward at 10.51 sec/img, corresponding to 2.46x wall-clock speedup. SADA obtains 0.3113 CLIPScore and 1.0808 ImageReward at 10.55 sec/img. Thus ZEUS preserves semantic alignment and preference quality on FLUX: CLIPScore is within 0.0004 of Full, ImageReward drops by only 0.0061, and at essentially the same latency as SADA, ZEUS achieves a higher ImageReward in this setting.

Second, to address the concern that changing the ODE integration trajectory may harm video temporal coherence, we added a balanced VBench evaluation on Wan2.1-T2V-14B. We used VBench 0.1.5 with 24 prompts, selecting 8 prompts each from the reviewer-relevant dimensions subject consistency, motion smoothness, and temporal flickering. At 480x832 resolution, 81 frames, and 50 steps, Full sampling obtains subject consistency 0.9619, motion smoothness 0.9810, temporal flickering 0.9737, and imaging quality 0.6118. ZEUS obtains 0.9628, 0.9820, 0.9755, and 0.6008, respectively. These results show that ZEUS does not degrade the three temporal metrics targeted by the reviewer; in fact, all three slightly improve, while imaging quality decreases only modestly by 0.0110. We will include the prompt list and explicitly state that this is a balanced VBench-24 rebuttal subset, not the full 946-prompt VBench benchmark.

Regarding SADA and other adaptive methods, we agree that adaptive budget allocation is a different axis from our fixed output-level reuse strategy. Our claim is not that ZEUS subsumes adaptive schedulers or dominates every operating point. Rather, ZEUS asks a different question: how much output-level information is sufficient for acceleration under evaluation scarcity, using a simple recipe without training, a learned policy, per-model calibration, or architecture-specific cache design. To avoid ambiguity, we will emphasize real end-to-end wall-clock latency rather than nominal acceleration labels. The new FLUX timing result is an example: ZEUS and SADA have essentially identical same-GPU latency, while ZEUS gives higher ImageReward in that setting. For SD-family cases where SADA has stronger single-metric quality at nearby speedups, we will state this clearly and frame ZEUS as a simpler quality-speed-memory tradeoff rather than a pointwise replacement for adaptive methods.

We also added an SDXL matched-budget comparison because several comments focus on legacy latent diffusion models. On 200 COCO prompts, Full/SADA/ZEUS obtain CLIPScore 0.3170/0.3171/0.3166 and ImageReward 0.7550/0.7417/0.7339. In a separate same-GPU sequential timing pass, Full/SADA/ZEUS use 50.0/25.2/24.0 actual NFE and take 3.366/1.556/1.464 sec/img. Thus SADA has slightly higher ImageReward on this SDXL setting, while ZEUS is about 6% faster than SADA with nearly identical CLIPScore. We will report this candidly as a fair speed-quality tradeoff.

To probe the limit boundary of interleaved scheduling, we also ran a consecutive-skip stress test on 200 SDXL COCO prompts on one GPU. Under the default anchor schedule, increasing max_interval from 2 to 8 yields observed maximum consecutive skips of 2, 4, 5, and 5, respectively, with CLIPScore around 0.3166-0.3171 and ImageReward 0.7339-0.7631. Since the default anchor interval itself caps longer runs, we additionally extended the anchor interval: 7 consecutive skips remains close to default quality (CLIP 0.3167, ImageReward 0.7366), while 11 consecutive skips causes a large ImageReward drop to 0.5599. We will clarify that ZEUS is intended for the practical moderate-acceleration regime, not arbitrary long cache intervals.

For the complementarity claim, we agree that joint measurements with sparse attention or cache methods are needed to support empirical additivity. Our intended claim is architectural: ZEUS modifies the sampling schedule and output-level denoiser reuse, whereas token/attention sparsification reduces per-call computation inside the denoiser. We will therefore soften the wording to "potentially complementary" and avoid implying guaranteed additive speedups.

We also added a lightweight sparse-attention composition pilot on SDXL. Since optimized sparse kernels are outside the scope of this rebuttal, we used naive top-k cross-attention sparsity only to test error compounding. Across 200 COCO prompts, CLIPScore remains stable for sparse-only and ZEUS+sparse settings: Full/ZEUS/Sparse20/ZEUS+Sparse20/Sparse50/ZEUS+Sparse50 obtain 0.3170/0.3166/0.3176/0.3170/0.3173/0.3171. ImageReward shows no catastrophic collapse, though the 20% sparse composition has a modest additional drop. We will therefore revise the claim to "potentially complementary" and report this as a small sanity check, with optimized joint sparse-attention speedups left for future work.

For token-level sparsity, we found that the pruning criterion matters. A crude residual-norm token-freezing control degrades quality even without ZEUS, so we will not present it as positive evidence. We therefore added a more defensible update-norm proxy that freezes only low-update tokens according to `||output-input||`. At 5% token-update sparsity, Full/ZEUS obtain CLIPScore 0.3168/0.3163 and ImageReward 0.7157/0.6910; at 10%, CLIPScore remains stable at 0.3181/0.3178 but ImageReward drops to 0.6815/0.6291. This suggests that conservative token-update reuse does not cause semantic collapse with ZEUS, while preference quality is sensitive to more aggressive token freezing. We will present it as a compatibility sanity check, not as optimized token-pruning speedup.

Finally, we will clarify the "training-free" wording. By training-free we mean no model retraining, no learned policy, and no per-model calibration. The schedule/window parameters are deterministic default recipes rather than optimized per benchmark. We will also fix presentation issues in the revision, including pseudocode, notation for true vs predicted values, duplicated text, and figure label readability.

## Ultra-Compact Version

We added two reviewer-requested evaluations. On FLUX.1-dev with 200 COCO prompts, ZEUS preserves alignment and preference quality: Full/ZEUS/SADA obtain CLIPScore 0.3116/0.3112/0.3113 and ImageReward 1.0991/1.0930/1.0808. In a clean same-GPU timing pass, Full/ZEUS/SADA take 25.84/10.51/10.55 sec/img, so ZEUS gives 2.46x wall-clock speedup and matches SADA latency while yielding higher ImageReward. For video, we added a balanced VBench-24 run on Wan2.1-T2V-14B (VBench 0.1.5; 8 prompts each from subject consistency, motion smoothness, and temporal flickering; 480x832, 81 frames, 50 steps). Full obtains 0.9619/0.9810/0.9737/0.6118 for subject/motion/flicker/imaging, while ZEUS obtains 0.9628/0.9820/0.9755/0.6008. Thus ZEUS does not degrade the temporal metrics targeted by the reviewer; all three slightly improve, with a small imaging-quality drop of 0.0110.

We also clarify positioning. Adaptive methods such as SADA solve a different problem: deciding when the ODE is stable enough to adapt the budget. ZEUS instead studies output-level information reuse under evaluation scarcity, with no training, learned policy, per-model calibration, or architecture-specific cache design. We do not claim ZEUS subsumes adaptive schedulers or dominates every SD-family point; where SADA gives stronger single-metric quality, we will state it clearly and compare by real wall-clock latency rather than nominal labels. For sparsity/cache complementarity, we will soften the claim to "potentially complementary": the mechanisms are architecturally orthogonal, but empirical additivity requires joint experiments. We will also fix the presentation issues, add pseudocode, and clarify notation and the meaning of "training-free."
